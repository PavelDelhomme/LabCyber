{
  "name": "labcyber-platform",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-docs": "cp -r docs public/docs",
    "predev": "npm run sync-docs",
    "dev": "vite",
    "build": "npm run sync-docs && vite build && (cp -r data dist/ 2>/dev/null; cp -r docs dist/docs 2>/dev/null; true)",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.19.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@preact/preset-vite": "^2.8.0"
  }
}
