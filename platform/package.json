{
  "name": "labcyber-platform",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r data docs dist/ 2>/dev/null || true",
    "preview": "vite preview",
    "sync-docs": "cp -r docs public/docs"
  },
  "dependencies": {
    "preact": "^10.19.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@preact/preset-vite": "^2.8.0"
  }
}
