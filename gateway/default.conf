# Placeholder pour le script docker-entrypoint (10-listen-on-ipv6).
# La config réelle est dans /etc/nginx/nginx.conf (gateway Lab Cyber).
# Ce fichier n'est pas inclus par nginx.conf ; il évite uniquement le message "default.conf is not a file or does not exist".
server { listen 127.0.0.1:8081; server_name _; return 444; }
