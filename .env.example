# Ports du lab (docker compose). Si déjà utilisés : make down puis modifier.
# Plateforme (gateway) : http://127.0.0.1:GATEWAY_PORT
# Terminal (ttyd)      : http://localhost:TTYD_PORT
# Note : docker-compose.minimal.yml utilise 9080 et 7682 par défaut pour éviter les conflits.
GATEWAY_PORT=8080
TTYD_PORT=7681
